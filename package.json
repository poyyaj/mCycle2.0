{
    "name": "mcycle",
    "version": "1.0.0",
    "description": "Menstrual Cycle & PCOS Tracking App",
    "scripts": {
        "install:all": "cd server && npm install && cd ../client && npm install",
        "build": "npm run install:all && cd client && npm run build",
        "start": "cd server && node server.js"
    }
}